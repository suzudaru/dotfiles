function! s:toggle_transparence()
    if &transparency == 175
        set transparency=0
    else
        set transparency=175
    endif
endfunction
nnoremap <silent> <Space>oT :<C-u>call <SID>toggle_transparence()<CR>

highlight LineNr guifg=darkgray
